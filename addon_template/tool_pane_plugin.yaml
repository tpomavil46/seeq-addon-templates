plugin_prompts:
  type: str
  help: "\n\n ******* Tool Panel Plugin element configuration *******\n This element does not require additional configuration."
  default: Press enter to continue
  when: "{{ 'ToolPanePlugin' in elements_to_include }}"
